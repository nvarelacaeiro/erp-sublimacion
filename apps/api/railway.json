{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && npm install -g pnpm@9.15.9 && pnpm install && cd packages/shared && pnpm build && cd ../../apps/api && pnpm exec prisma generate && pnpm exec tsc"
  },
  "deploy": {
    "startCommand": "node ../../node_modules/.bin/prisma migrate deploy --schema=./prisma/schema.prisma && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
